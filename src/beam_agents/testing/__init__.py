"""Test-only infrastructure for exercising runtime correctness invariants.

Never re-exported from the root `beam_agents` API. See
:mod:`beam_agents.testing.chaos` for the bundle-retry chaos helper used by the
retry-determinism semantics gate.

Importing this package has no side effects.
"""
