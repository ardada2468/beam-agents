## 1. Tests (written first, must fail for the right reason)

- [x] 1.1 `tests/benchmarks/test_bench_gate.py`: the gate's two judgements over synthetic pyperf result files — a p50 ≥ 15 ms or p99 ≥ 60 ms overhead result exits non-zero naming the breached threshold and measured value ("A budget breach fails the gate"); a median beyond the baseline tolerance exits non-zero naming benchmark/baseline/tolerance/median ("A regression beyond tolerance fails the gate"); an improving median passes and prints the lower-the-baseline instruction ("An improvement prompts a deliberate baseline update"); an absent result file or one with too few samples for p99 exits non-zero ("Missing results are a failure, not a pass"); percentiles are computed over pooled per-activation values, not per-process aggregates ("Percentiles are computed over per-activation samples"). — 14 tests; synthetic results are written through pyperf's own `BenchmarkSuite.dump`, so the gate is exercised against the real JSON schema. The pooling test hides the whole 70 ms tail inside one worker process, so any per-process aggregation passes and only pooled values fail.
- [x] 1.2 `tests/benchmarks/test_bench_gate.py`: the tier-invariance judgement — a synthetic result set whose 2000 ms tier median overhead exceeds the 50 ms tier's by more than the tolerance fails, and one within tolerance passes ("Overhead is invariant to provider latency"). — `test_overhead_growing_with_provider_latency_fails_the_gate` (16 ms vs 4 ms, 10 ms tolerance) and `test_overhead_within_the_invariance_tolerance_passes` (12 ms); each pins the tier's own baseline to its measured value so only the invariance judgement can fail.
- [x] 1.3 `tests/benchmarks/test_bench_smoke.py`: import every `benchmarks/bench_*.py` module and execute each timed function for one iteration offline with no docker ("Benchmark modules cannot rot silently", "The full suite runs offline") — including one suspend-resume pair asserting the continuation was written then cleared through the real commit path ("A suspend-resume pair is timed as one round trip"), and the state-commit setup producing blobs at every configured size including 100 KiB ("Commit cost is reported across blob sizes up to the cap"). — module discovery is from the filesystem (`glob("bench_*.py")`), so a new benchmark cannot be added outside the smoke selection; a further test asserts the timed-function names equal the gate's `EXPECTED_RESULTS` set, so the two spellings of the surface cannot drift.
- [x] 1.4 `tests/benchmarks/test_harness.py`: the overhead arithmetic — with a scripted clock, the recorded value equals wall time minus the configured tier latency, and scheduling slop above the nominal sleep stays in the value ("Overhead subtracts the configured tier latency"); the `RunInference` comparison's report computes delta and ratio from the two per-element figures and marks the delta as the baseline-tracked quantity ("The comparison isolates the runtime's cost over raw inference"). — 4 tests over `overhead_sample` and `bench_gate.comparison_summary`/`BASELINE_TRACKED`.
- [x] 1.5 Confirm all of the above fail for the right reason before implementation: missing modules/`bench_gate.py`, not assertion typos. — verified: `ImportError: cannot import name 'bench_gate' from 'scripts'` and `ModuleNotFoundError: No module named 'benchmarks'` for all three test modules at collection.

## 2. Harness skeleton

- [x] 2.1 Create `benchmarks/__init__.py` and `benchmarks/_harness.py`: the no-op, single-call, suspending, and memory-writing module-level agents; `FakeLLM` scripts per tier (`respond_with(payload, latency_ms=tier)` with the default real-sleep delay); in-memory state/timer handles (deliberately duplicated from `tests/core/_dofn_fakes.py` — benchmarks must not import the test tree; design D1); tier and size constants; and the `_AgentDoFn` construction helper (fake handles, `FakeLLM` provider factory). — `Handles` bundles the seven handles with a `kwargs()` accessor in `process()` keyword order; agents and provider factories are module-level so they pickle by reference into the DirectRunner pipelines.
- [x] 2.2 Add `benchmarks` to `[tool.mypy].files` in `pyproject.toml` with the same Beam-untyped-API relaxations the DoFn-driving test modules carry; add `bench-results/` to `.gitignore`. — `[[tool.mypy.overrides]] module = "benchmarks.*"` disables `no-untyped-call`/`type-arg`/`no-any-return`; pyperf ships no `py.typed`, so it also gets `ignore_missing_imports`. `.gitignore` covers `bench-results/` and the generated `bench-report.md`. `benchmarks/bench_*.py` gets a `PLC0415` per-file-ignore: pyperf is imported inside `main()` on purpose, so the modules import (and the smoke tests run) in the `ci` lane, which never syncs the `bench` group.

## 3. The five benchmarks

- [x] 3.1 `benchmarks/bench_noop_throughput.py`: one full `process()` drain per value with the no-op agent; report time per activation (activations/sec derived in the report). — verified on this box (2 processes × 3 values, deliberately reduced): **158 µs ± 18 µs**, median 0.1512 ms ⇒ ~6,615 activations/sec.
- [x] 3.2 `benchmarks/bench_overhead_tiers.py`: one activation per value (`--loops 1`), value = wall time minus nominal tier latency; dense sampling (≥ 1000 values across ~20 worker processes) on the 50 ms tier, reduced sampling on 500/2000 ms; one pyperf benchmark per tier in one JSON file. — per-tier sampling density needs per-tier worker settings, which one pyperf invocation cannot express, so running the module without `--tier` orchestrates one `--append` run per tier into the single JSON (see Revision 1). `TIER_SETTINGS` pins 50 ms at 20 processes × 50 values = 1000 samples; `--fast` scales down for local iteration only. Verified with `--fast`: medians **50 ms 1.046 ms · 500 ms 1.531 ms · 2000 ms 3.112 ms** (this box is a noisy 4-core VM — pyperf itself flagged the run unstable; these numbers are evidence the harness runs, NOT baseline candidates).
- [x] 3.3 `benchmarks/bench_suspension_roundtrip.py`: per value, one Suspend-committing activation plus one admitted `ToolResult` resume over shared handles; effector and transport excluded. — the intent ID is derived with `intent_id_for(KEY, seq, 0)` off the SEQ handle, so the resume envelope is built with no effector in the loop; envelope construction sits between the two clocked windows. Verified: **629 µs ± 151 µs**.
- [x] 3.4 `benchmarks/bench_state_commit.py`: full activations committing working memory at 1/16/64/100 KiB, plus the encode-only micro-benchmark over `DeterministicProtoCoder.encode` at the same sizes. — verified: commits **165 / 176 / 172 / 200 µs**, encode-only **0.60 / 1.17 / 3.70 / 5.52 µs**. The attribution the dimension exists for is already visible: the encode curve is ~linear in size and is ~3% of activation cost even at the 100 KiB cap, so the (flat) commit curve is staging, not the deterministic encode.
- [x] 3.5 `benchmarks/bench_runinference_compare.py`: the two DirectRunner pipelines (`RunAgent` vs `RunInference` with the minimal `FakeLLM` `ModelHandler`), identical inputs, zero-latency behaviors; report per-element cost, delta, and ratio. — 100 elements per pipeline with pyperf `inner_loops=100`, so recorded values are per-element seconds and the runner's fixed startup is spread identically on both sides. Verified: RunAgent **2.08 ms** vs RunInference **1.60 ms** per element ⇒ delta 0.446 ms, ratio 1.27x.

## 4. Gate and baseline

- [x] 4.1 `scripts/bench_gate.py`: load results via pyperf's `Benchmark` API (import-the-authority, loud ImportError — the `mutation_gate.py` stance); judgement 1: absolute overhead budget p50 < 15 ms / p99 < 60 ms on the 50 ms tier's pooled values; judgement 2: per-benchmark median vs `benchmark-baseline.toml` with the file's tolerance band, including the tier-invariance check and the `RunInference` delta; fail on missing/short results; print the ratchet instruction on improvement. — the two judgements accumulate independently and both are reported; a missing result file, a declared benchmark absent from a file, fewer than 1000 pooled gated samples, or an unseeded baseline entry each fail loudly.
- [x] 4.2 `benchmark-baseline.toml` at the repo root (sibling to `coverage-baseline.toml`/`mutation-baseline.toml`): per-benchmark median baselines seeded from a manual full run on a GitHub-hosted runner (runner generation noted in the file's comment), plus the named tolerance constant. — **seeded 2026-08-03** from the scheduled nightly `bench` job, run 30877576573's predecessor **30806138398** (`ubuntu-latest`, `main` @ `e5cf356`, 2026-08-03T10:34:55Z): all 14 `seed medians_ms.<name> = <value>` instructions printed by that run's red gate were copied verbatim into `[medians_ms]`, with the run id and runner generation noted in the file's comment. The red-by-construction behavior the original annotation predicted is exactly what that run exhibited (its `Run benchmarks` step succeeded; only the `Benchmark gate` step failed, on the 14 unseeded entries). `scripts/bench_gate.py` over that run's downloaded `benchmark-report` artifact now exits 0 against the seeded file: absolute budget p50 0.8517 ms (< 15 ms) / p99 0.9767 ms (< 60 ms) on 1000 pooled samples, every median within tolerance by construction.
- [x] 4.3 Report generation in `bench_gate.py` (single reader of the JSON): render `bench-report.md` with per-benchmark medians/percentiles, the tier-invariance table, the `RunInference` delta, gate verdicts, and pyperf's environment metadata. — rendered from the real local run: verdict banner, the gated tier's p50/p99 with sample count, the median table with derived activations/sec, the invariance table, the delta/ratio, every `FAIL:`/ratchet line, and pyperf's captured host/CPU/Python metadata. A percentile the run had too few samples for renders as "not computed (fewer than 1000 pooled samples)", never as `nan` (a `nan` in a released report invites being read as zero).

## 5. CI, Makefile, docs

- [x] 5.1 `Makefile`: `bench` (run all benchmark modules, JSON into `bench-results/`) and `bench-gate` (`uv run python scripts/bench_gate.py`) targets with `## ` help lines. — `BENCH_ARGS` is the local-iteration knob (`make bench BENCH_ARGS=--fast`); CI passes nothing, so the modules' pinned sampling constants apply.
- [ ] 5.2 `.github/workflows/nightly.yml`: a `bench` job (schedule + `workflow_dispatch`) that syncs the `test` and `bench` groups, runs `make bench` then `make bench-gate`, and uploads `bench-results/*.json` + `bench-report.md` as one stably named artifact; budget ≤ 15 `timeout-minutes`. — job is written (`timeout-minutes: 15`, `uv sync --locked --group test --group bench`, `make bench` → `make bench-gate`, `actions/upload-artifact@v4` with `name: benchmark-report`, `if: always()`, `if-no-files-found: error`), but **(blocked: needs CI)** for the verification that it actually runs green on a hosted runner. — *2026-08-03 update:* the job now has scheduled hosted-runner executions (runs 30741073228, 30806138398): the measurement half runs and uploads the artifact; the gate half went red on the unseeded baseline exactly as designed. The first fully green run needs the seeded `benchmark-baseline.toml` (task 4.2) merged to `main`, then the next scheduled/dispatched run closes this box.
- [x] 5.3 `docs/benchmarks.md`: what each dimension measures and deliberately excludes (fake handles vs runners, effector out of the round-trip), how to run locally, how to read the report, and the baseline-update procedure; add the bench row to `docs/ci.md`'s workflow map and point `docs/metrics.md`'s "belongs to the benchmark suite" note at the suite. — the measures/excludes table covers all five dimensions; `docs/ci.md` gains both the nightly-row edit and a "benchmark lane" section; `docs/metrics.md`'s note now links the suite and states that its `overhead_*ms` tiers record the same subtraction.
- [x] 5.4 Note the artifact's stable name where the sibling `add-0-1-0-release` change consumes it (release attaches the most recent green report). — the name is **`benchmark-report`**, stated in `docs/benchmarks.md` ("The release artifact"), in `docs/ci.md`'s benchmark-lane section, and in a comment on the workflow's upload step.
- [x] 5.5 Verify the gate carries no `xfail`/retry/skip-when-red tolerance, and that CI pins sampling parameters (env knobs are for local iteration only) — the discipline the e2e gate established. — verified: no `xfail`/`skipif`/retry anywhere in `scripts/bench_gate.py` or the workflow job, and every failure path returns 1 rather than warning. Sampling constants live in the benchmark modules (`PROCESSES`/`VALUES`/`WARMUPS`, `TIER_SETTINGS`); the workflow passes no `BENCH_ARGS` and no env var, so `--fast` and CLI overrides can only ever affect a local run.

## 6. Gates

- [x] 6.1 `make lint` and `make type` clean (`benchmarks/` now inside the mypy file set). — `ruff check` + `ruff format --check` pass over 212 files; `mypy --strict` reports "Success: no issues found in 209 source files" with `benchmarks` in `files`.
- [x] 6.2 `make test-unit` passes offline, smoke tests included, within the unit tier's timeout. — **973 passed, 1 skipped** (the pre-existing aiokafka skip) in 93.56s; total coverage 97.79%. The one benchmark test that runs whole DirectRunner pipelines carries `slow` + `timeout(240)`.
- [x] 6.3 `make coverage-ratchet` at or above baseline (`benchmarks/` and `scripts/` sit outside the coverage source; re-measure and lock in any movement per the ratchet procedure). — "branch coverage 94.84% is at baseline"; unmoved, as expected: `[tool.coverage.run] source = ["beam_agents"]` excludes both new trees.
- [ ] 6.4 One full `make bench && make bench-gate` run green locally and one on a hosted runner via `workflow_dispatch`, with the seeded baseline committed. — **(blocked: needs CI)** all five modules were executed end to end here with deliberately reduced sampling (`--processes 2 --values 3`, `--fast` for the tiers) and `make bench-gate` ran over the real JSON, proving the suite executes and the report renders — see the numbers in §3. It is red by design on two counts, both requiring quiet CI hardware to clear: the reduced run has 40 pooled gated samples (< 1000), and `[medians_ms]` is deliberately unseeded (task 4.2). A full-density local run is not the deliverable this task asks for — the baseline it would produce must not be committed from this box. — *2026-08-03 update:* the hosted-runner half is done in substance: run 30806138398 executed `make bench` at full density on `ubuntu-latest` (1000 pooled gated samples), its artifact was pulled down, the baseline was seeded from it (task 4.2), and `bench_gate.py` over that same JSON now exits 0. What remains is the formality the box literally names — one hosted run that is green end-to-end *in CI*, which requires the seeded baseline to merge first; left unchecked with 5.2 until that run exists. The "green locally" half is deliberately not re-litigated: local medians on developer hardware can sit outside the 25% band around CI-seeded baselines (this host previously measured overhead_50ms at 2.13 ms vs CI's 0.85 ms), which is a property of the hardware split, not a regression.
- [x] 6.5 `uv run pre-commit run --all-files` clean. — **(blocked: needs CI)** the `precommit` dependency group is outside the synced set for this run (`lint`/`typecheck`/`test` only, plus `pyperf`), and installing it would move `uv.lock`. Its constituent checks were run directly instead and are green: `make lint`, `make type`, and the OpenSpec change validation below. <!-- discharged by verify-live-infrastructure phase 0 (2026-07-31): `uv run pre-commit run --all-files` executed on the merged tree, all 10 hooks passed (ruff, ruff-format, check-yaml, check-toml, end-of-file-fixer, trailing-whitespace, mypy, protobuf-drift, openspec-change-required, changelog-fragment-required). See verification-report.md. -->
- [x] 6.6 `openspec validate add-benchmark-harness --strict` passes. — `npx @fission-ai/openspec@1.7.0 validate add-benchmark-harness --strict` green.

## Revision 1 — one JSON file per benchmark module, with per-tier orchestration inside `bench_overhead_tiers`

Task 3.2 asks for "one pyperf benchmark per tier in one JSON file", and the gate's `EXPECTED_RESULTS` maps one file per module. Implementation surfaced that a single `pyperf.Runner` invocation carries **one** set of worker settings (`--processes`/`--values`/`--warmups`) for every benchmark it registers, while design D2 requires deliberately *different* densities per tier: ~1000 samples on the gated 50 ms tier and far fewer on 500/2000 ms, because a full-density 2000 ms tier would cost half an hour of wall clock to learn nothing new.

Registering all three tiers on one `Runner` would therefore force one density on all three — either a 2000 ms tier that dominates the suite's runtime budget, or a 50 ms tier too thin for a p99. Neither is acceptable; the p99 is the whole point of the gated tier, and the ≤ 10-minute suite budget is a stated risk mitigation.

Resolution, entirely inside the module and invisible to the gate: running `benchmarks.bench_overhead_tiers` without `--tier` orchestrates one subprocess `pyperf` run per tier, each with its own pinned `TIER_SETTINGS` entry and `--loops 1`, all appending into the single `--output` JSON. `--tier <ms>` is the per-tier entry point (manager and worker alike, carried into workers via pyperf's `add_cmdline_args`). The artifact contract is unchanged — `bench-results/bench_overhead_tiers.json` holds exactly the three named benchmarks — so no spec, design decision, or gate constant moved. No artifact edit was required for this; it is recorded here because the module's `main()` is structurally unlike the other four.

## 8. Revision: keep the gate importable without the `bench` group (integration)

- [x] 8.1 `scripts/bench_gate.py` raised `SystemExit(1)` at import when pyperf was missing, but the
  required `ci` unit lane syncs only lint/typecheck/test — never `bench`. `tests/benchmarks/`
  imports the module, so on that lane pytest aborted collection with an INTERNALERROR
  (`SystemExit` during collection), taking the whole required lane down. Moved the loud message
  and non-zero exit into `main()` and left a module-level `_PYPERF_IMPORT_ERROR` sentinel: the
  CLI still fails loudly with the same text, while importing stays safe. This preserves the
  design's own intent — `test_bench_smoke.py` needs only `EXPECTED_RESULTS` (no pyperf), so the
  benchmark-rot check keeps running in the required lane, which is exactly why D1 put it there.
- [x] 8.2 `tests/benchmarks/test_bench_gate.py` genuinely builds `pyperf.Benchmark` objects, so it
  now `pytest.importorskip`s pyperf (with a `TYPE_CHECKING` import so `--strict` still resolves
  the annotations) and skips on the unit lane instead of erroring. Verified both ways: without
  pyperf 13 passed / 1 skipped; with pyperf 27 passed. `make lint`, `make type` clean.
