.next
.next-build
node_modules
generated
pnpm-lock.yaml
examples
# `next build` rewrites tsconfig.json — it adds its own `include` entries and
# expands the file in the process. Prettier wants it collapsed again, so with
# the file checked, `pnpm lint` passes on a fresh checkout and fails the moment
# anyone builds. Next owns this file; the formatter does not get a vote.
tsconfig.json
# MDX content is authored prose. Prettier reflows it (and rewrites tables and
# JSX inside it), which makes content diffs unreadable and risks changing what
# a page says. Content correctness is the fidelity checks' job, not a
# formatter's.
content
