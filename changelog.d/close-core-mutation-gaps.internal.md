Closed the `core/` mutation-testing debt the first working `make mutation` run exposed: 144 unkilled
mutants and one indeterminate timeout are now killed by tests derived from their owning spec
scenarios, or declared equivalent with a written reason. Test-only — no runtime behavior changed.
