import { Placeholder } from '@/pages/_Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Entity key"
      describes="one key's timeline across every sequence it has run"
    />
  );
}
