import { Placeholder } from '@/pages/_Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Entity keys"
      describes="every entity key the runtime has activated, with its activity across sequences"
    />
  );
}
