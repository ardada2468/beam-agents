import { Placeholder } from '@/pages/_Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Tools"
      describes="per-tool call volume, failure rate, and the split between inline calls and staged intents"
    />
  );
}
