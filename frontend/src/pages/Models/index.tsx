import { Placeholder } from '@/pages/_Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Models"
      describes="per-model call volume, token spend, cache-hit ratio, and circuit state"
    />
  );
}
