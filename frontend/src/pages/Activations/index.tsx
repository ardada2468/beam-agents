import { Placeholder } from '@/pages/_Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Activations"
      describes="every activation keyed by entity key and sequence, with status, model, tokens, and tool calls"
    />
  );
}
