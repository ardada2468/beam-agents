import { Placeholder } from '@/pages/_Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Activation"
      describes="one activation's span sequence, event attributes, staged intents, attempts, and errors"
    />
  );
}
