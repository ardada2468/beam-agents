import { Placeholder } from '@/pages/_Placeholder';

export default function Page() {
  return (
    <Placeholder
      title="Errors"
      describes="errors grouped by the runtime's reason vocabulary, with failure position for the routes that record it"
    />
  );
}
