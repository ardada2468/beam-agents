"""A quickstart that runs against real infrastructure.

Unlike the other examples, this one calls a real model provider by default and
delivers what it records to a running console. See `docs/quickstart.md`.

Sample code, not a supported runtime surface: nothing here enters the wheel or
`beam_agents.__init__`.
"""
